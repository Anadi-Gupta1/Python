import matplotlib.pyplot as plt
import numpy as np

# Fix: Both arrays need same number of points
x_points = np.array([0, 0.6])  # Two x-coordinates
y_points = np.array([0, 250])  # Two y-coordinates
plt.plot(x_points, y_points)
plt.show()

import matplotlib.pyplot as plt
import numpy as np

# Fix: Both arrays need same number of points
x_points = np.array([0, 0.6])  # Two x-coordinates
y_points = np.array([0, 250])  # Two y-coordinates
plt.bar(x_points, y_points)
plt.show()


import matplotlib.pyplot as plt
import numpy as np

# Fix: Both arrays need same number of points
x_points = np.array([0, 0.6])  # Two x-coordinates
y_points = np.array([0, 250])  # Two y-coordinates
plt.hist(x_points, y_points)
plt.show()



import matplotlib.pyplot as plt
import numpy as np

# Fix: Both arrays need same number of points
x_points = np.array([0, 0.6])  # Two x-coordinates
y_points = np.array([0, 250])  # Two y-coordinates
plt.pie(x_points, y_points)
plt.show()


import matplotlib.pyplot as plt
import numpy as np

# Fix: Both arrays need same number of points
x_points = np.array([0, 0.6])  # Two x-coordinates
y_points = np.array([0, 250])  # Two y-coordinates
plt.bar(x_points, y_points)
plt.show()


import matplotlib.pyplot as plt
import numpy as np

xpoints = np.array([1, 2, 6, 8])
ypoints = np.array([3, 8, 1, 10])

plt.plot(xpoints, ypoints)
plt.show()


import matplotlib.pyplot as plt
import numpy as np

ypoints = np.array([3, 8, 1, 10, 5, 7])

plt.plot(ypoints)
plt.show()